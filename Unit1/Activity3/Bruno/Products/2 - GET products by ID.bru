meta {
  name: 2 - GET products by ID
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/products/1
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.id: eq 1
}

tests {
  
  expect(res.body).to.be.an('object');
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
