meta {
  name: 1 - GET products
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/products
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  Array.isArray(res.body): eq true
}

settings {
  encodeUrl: true
  timeout: 0
}
